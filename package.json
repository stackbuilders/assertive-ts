{
  "name": "assertive-ts",
  "version": "0.0.0",
  "description": "A type-safe fluent assertion library",
  "repository": "git@github.com:stackbuilders/assertion-library.git",
  "author": "Stack Builders <info@stackbuilders.com>",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.prod.json",
    "check": "yarn compile && yarn lint && yarn test --forbid-only",
    "compile": "tsc -p tsconfig.json",
    "lint": "tslint -c tslint.json \"!(dist)/**/*.ts\"",
    "test": "cross-env NODE_ENV=test TS_NODE_TRANSPILE_ONLY=true mocha"
  },
  "devDependencies": {
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.15",
    "@types/sinon": "^10.0.11",
    "all-contributors-cli": "^6.20.0",
    "cross-env": "^7.0.3",
    "mocha": "^9.2.0",
    "sinon": "^13.0.1",
    "ts-node": "^10.5.0",
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  },
  "packageManager": "yarn@3.1.1"
}
